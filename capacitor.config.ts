import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joni.gestordetareas',
  appName: 'Gestor de Tareas',
  webDir: 'dist'
};

export default config;
